# EmojiLabeler 安装与使用说明

## 安装 APK
1. 把 app-debug.apk 传到手机（微信文件传输助手 / QQ / 网盘均可）
2. 在手机上点开该文件 → 允许「安装未知来源应用」（各品牌手机提示不同，按提示允许即可）
3. 安装完成后打开「表情标注」

## 使用流程
1. **导入**：首页 → 导入新文件夹 → 选择存表情包的文件夹
   （建议先把要标的图都放到一个文件夹里，可含子文件夹）
2. **标注**：看图 → 点下方 emoji → 自动下一张
   - 撤销/上一张/跳过/下一张在底部
   - 双击大图可放大看细节
3. **自定义 emoji**：首页 → 设置 → 添加/删除你需要的 emoji（只显示你选的）
4. **导出**：标完后首页 → 导出标注结果 → 选一个导出位置
   - 生成 😂/😭/... 子文件夹 + labels.json + stats.json
   - 把这个文件夹拷到电脑 = 训练数据（dataset_raw）

## 常用设置
- 标注后自动下一张：默认开，可关
- 多标模式：一张图标多个 emoji 时打开
- 存储模式：默认「复制到 App 内」；空间紧张可切「原地移动」